# Giver 대원칙

## 수학적 정의

### 시그니처

| 역할 | 시그니처 |
|------|----------|
| G | `user_input → H` |
| P | `H → H` |
| S | `H → H` |
| W | `H → H` |

### 데이터 구조

| 요소 | 정의 | 비고 |
|------|------|------|
| T | O + C + F[] + L[] | G가 작성 (초기 T) |
| Tₖ | O + C + F[] + L[] + TF + D₀ | P가 Wₖ용으로 큐레이팅 |
| D | (sig, path) | 시그니처 + 파일경로 튜플 |
| D₀ | D[]의 큐레이팅 | 초기 D[]에서 이 W에 맞게 |
| TF | 파일목록 | 최대 3개 |
| R | ok + msg + D[] | 1/0, 자유 텍스트, 새 시그니처 |

### 역할 구성
> Giver G, Planner P, Scout S, Worker W 이다.

### 대원칙

1. G는 전체 컨텍스트 레벨에서 사용자와 대화하는 역할을 가진다.
2. G는 작업을 지시받으면 수행에 필요한 정보만 모아서 T를 작성한다. G는 대화 중에 D[]를 만들 수도 있다.
3. T = O + C + F[] + L[] (G가 작성하는 초기 T)
4. Tₖ = O + C + F[] + L[] + TF + D₀ (P가 Wₖ용으로 큐레이팅)
5. subagent P, S, W는 모두 **context fresh 모드**로 실행된다.
6. P는 H를 관리한다. JB/TB 대신 T를 큐레이팅하여 각 W에 맞게 O, C, F[], L[]를 추린다.
7. S는 H를 입력으로 받아 의존성을 수집한다.
8. TF는 최대 3개 파일까지.
9. D₀는 초기 D[]에서 이 W에 맞게 큐레이팅한 것. W에서 나온 새 D[]는 큐레이팅 없이 전부 누적.
10. D = (sig, path)
11. R = ok + msg + D[] (ok: 1/0, msg: 자유 텍스트)
12. 체인 실행 중 하나의 H가 수직으로 누적된다. 각 에이전트는 이전 H를 받아서 자기 출력을 append 한다.
13. R.ok=1이면 다음 Tₖ를 작성하고 H에 append. W에서 나온 새 D[]는 큐레이팅 없이 전부 누적. 초기 D[]만 D₀로 큐레이팅.
14. R.ok=0이면 수행 중단, H를 G에게 리턴.
15. W[] 모두 성공하면 P는 G에게 최종 H를 리턴.
16. G는 H를 해석해서 사용자에게 보고.

---

## 구현체

### H 문서 형식

```markdown
## T₀
### O
### C
### F[]
### L[]
### D[]

---
## 0
### T₀
### R₀

---
## 1
### T₁
### R₁

---
...
```

### 체인 흐름

```
사용자 ↔ G (대화, 결정)
           ↓
      G → T₀ 작성 + D[] 수집
           ↓
      H = T₀ + D[]
           ↓
      P (fresh, H 입력)
       │
       ├→ S (fresh, H) → H에 R append (D[] 수집)
       │
       ├→ T₀ 작성 → H에 append
       ├→ W₀ (fresh, T₀ + H) → H에 R₀ append
       │
       ├→ T₂ 작성 → H에 append
       ├→ W₂ (fresh, T₂ + H) → H에 R₂ append
       │
       ├→ ... 순차 실행
       │
       ├→ R.ok=0 → 중단, H를 G에 리턴
       └→ 전부 성공 → H를 G에 리턴

      G → 사용자 보고
```

### P의 H 처리

1. R.ok 확인 → 성공(1)/실패(0) 판단
2. 성공 시: R의 D[]를 H에 누적 (큐레이팅 X)
3. 성공 시: 다음 Tₖ를 작성하여 H에 append
4. 실패 시: H를 그대로 G에게 리턴, 실행 중단
5. 전부 성공 시: 최종 H를 G에게 리턴

### D의 두 출처

- **D₀** — 초기 D[]에서 P가 큐레이팅 (계획 시점에 알던 것)
- **R.D[]** — W가 새로 만든 의존성 (실행 중에 생긴 것, 큐레이팅 없이 전부 누적)